@model IEnumerable<TrashCollector.Models.Customer>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.pickUpDay)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.pickUpDay)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.userNameID }) |
            @Html.ActionLink("Details", "Details", new { id=item.userNameID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.userNameID })
        </td>
    </tr>
}

</table>
